# @sandbox-core/adapter-memory

In-memory backend for tests, examples, and early SDK development.

Important:

- this is not a real isolation backend
- this is not suitable for production workloads
- it exists so the shared contract can be exercised end to end before Docker and Azure implementations land
