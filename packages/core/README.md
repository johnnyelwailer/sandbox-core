# @sandbox-core/core

Portable sandbox contract and backend registry.

This package owns:

- the core domain model
- the `SandboxBackend` interface
- the `Sandbox` interface
- the registry used to select and create backends
- portable error categories

This package must remain backend-neutral.
