# @sandbox-core/sdk-typescript

TypeScript-first SDK for `sandbox-core`.

It re-exports:

- core types and registry
- schema documents

This package is the primary entry point for TypeScript consumers.
