# @sandbox-core/adapter-azure

Azure backend scaffold.

This package is expected to become the first production hosted backend, while keeping Azure-specific concerns fully outside `@sandbox-core/core`.
